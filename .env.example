# Backend CORS origins (comma-separated).
# If not set, defaults to "*" for local development.
# For production, set to your frontend domain(s), e.g.:
# BACKEND_CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com
BACKEND_CORS_ORIGINS=*
