"""
CLI Entrypoint Module

This will become the CLI entrypoint for the factory simulator demo:
- Parse user intent from command line or stdin
- Call run_pipeline(user_text) from orchestrator
- Print briefing markdown to stdout

For now: minimal stub, placeholder only.
"""


if __name__ == "__main__":
    print("factory simulator demo – implementation coming in later PRs.")
